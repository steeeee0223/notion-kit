---
title: Icon Block
description: A block for icon, emoji, or image display.
---

<ComponentPreview
  name="icon-block/emoji"
  preview={`<IconBlock size="md" icon={{ type: "emoji", emoji: "🚀" }} />`}
/>

## Installation

```package-install
pnpm add @notion-kit/icon-block
```

## Options

---

### Size

Use the `size` prop to control the size of the button.

<ComponentPreview
  name="icon-block/sizes"
  preview={`<IconBlock size="sm" icon={icon} />
  <IconBlock size="md" icon={icon} />
  <IconBlock size="lg" icon={icon} />
  <IconBlock size="xl" icon={icon} />`}
/>

## Examples

---

### Lucide Icon

<ComponentPreview
  name="icon-block/lucide"
  preview={`<IconBlock
    size="md"
    icon={{ type: "lucide", name: "badge-euro", color: "#3e9392" }}
  />`}
/>

### Image Url

<ComponentPreview
  name="icon-block/url"
  preview={`<IconBlock
    size="md"
    icon={{ type: "file", url: "https://github.com/shadcn.png" }}
  />`}
/>

### Text

<ComponentPreview
  name="icon-block/text"
  preview={`<IconBlock size="md" icon={{ type: "text", text: "John" }} />`}
/>

## API Reference

### IconBlock

| Prop       | Type                           | Default | Description                                                     |
| ---------- | ------------------------------ | ------- | --------------------------------------------------------------- |
| `icon`\*   | `IconData`                     | -       | The icon data.                                                  |
| `size`     | `"sm" \| "md" \| "lg" \| "xl"` | `"sm"`  | The size of the block.                                          |
| `fallback` | `string`                       | -       | The fallback string to display when an image can not be loaded. |

### `type` IconData

There are 4 types of icons:

1. `emoji`: An emoji icon, whose `src` is simply the emoji string. See [Emoji Cheat Sheet](https://emojipedia.org/).
2. `lucide`: A Lucide icon, whose `src` is the name of the icon. See [Lucide Icons](https://lucide.dev/icons).
3. `url`: An image URL. The image will be displayed in the block.
4. `text`: A text string. Only the first letter of `src` will be displayed in the block.

| Prop     | Type                                     | Description                     |
| -------- | ---------------------------------------- | ------------------------------- |
| `type`\* | `"emoji" \| "lucide" \| "url" \| "text"` | The icon type.                  |
| `src`\*  | `string`                                 | The source of the icon.         |
| `color`  | `string`                                 | The color of the `lucide` icon. |
